> add all designs of html page which has been designed using adobe xd. and also add the required assets or images which will be required to build the page according to design.

> the whole page design & assets will be exported in the format of image either jpg or png or svg.

> also add the colors in which is used in the design. store color in .txt file [ name format will be <page-name>colors.txt].
